{
  "mcpServers": {
    "doc": {
      "command": "python",
      "args": ["mcp/doc/server.py"],
      "env": {
        "DOC_MCP_LOG": ".caw-agent/doc-mcp.log"
      },
      "description": "Documents — extract text from PDF / Word / Excel / PowerPoint"
    }
  }
}

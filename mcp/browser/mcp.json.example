{
  "mcpServers": {
    "browser": {
      "command": "python",
      "args": ["mcp/browser/server.py"],
      "env": {
        "BROWSER_MCP_LOG": ".caw-agent/browser-mcp.log"
      },
      "description": "Headless Chromium — snapshot, click, type, screenshot (Playwright)"
    }
  }
}
